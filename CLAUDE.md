# CLAUDE.md

## Project Overview

StoryAI 是一个基于 Claude Code Skill 体系的 AI 长篇小说写作助手系统。9 个模块化 Skill 覆盖从世界观构建到整本输出的完整写作流水线。

**核心定位**：纯 Claude Code 原生，无需外部服务器、数据库或 LLM API。所有创意生成由 Claude 完成，辅助脚本使用 Python 3 标准库。

## Directory Structure

```
StoryAI/
├── CLAUDE.md                          # 本文件
├── .claude/skills/                    # 9 个 Claude Code Skill
│   ├── pipeline-orchestrator/         # 全流程编排
│   ├── world-builder/                 # 世界观构建
│   ├── character-designer/            # 角色设计
│   ├── plot-planner/                  # 剧情规划
│   ├── chapter-writer/                # 章节写作
│   ├── quality-checker/               # 质量控制
│   ├── ai-humanizer/                  # 去 AI 味
│   ├── consistency-tracker/           # 一致性追踪
│   └── grill-with-docs/               # 拷打式评审（大纲+章节）
├── shared/
│   ├── rules/                         # 通用写作规则（适用于所有小说）
│   ├── scripts/                       # 共享 Python 工具（纯 stdlib）
│   ├── templates/                     # 输出结构模板
│   └── references/                    # 参考标准文档
└── novels/
    └── 曼巴重生/                       # 每本小说独立目录
        ├── chapters/                   # 章节正文
        ├── .storyai/                   # 本小说运行时状态 + 特定规则
        ├── world_settings.md
        ├── characters.md
        └── plot_outline.md
```
**多小说支持**：`shared/` 是通用层，每本小说在 `novels/<书名>/` 下拥有独立的一切（章节、设定、规则、状态）。

## How to Use

### 方式一：全流程编排

```
"我要写一本小说" / "开始写小说" / "从零开始写一本玄幻小说"
```

自动触发 `pipeline-orchestrator`，引导完成：项目设置 → 世界观 → 角色 → 大纲 → 逐章写作循环。

### 方式二：独立调用 Skill

| 你说的话 | 触发的 Skill |
|---------|-------------|
| "构建世界观" / "世界观设定" | world-builder |
| "设计角色" / "人物设定" | character-designer |
| "规划大纲" / "拆解大纲" | plot-planner |
| "写第5章" / "续写下一章" | chapter-writer |
| "审核第5章" / "看看质量" | quality-checker |
| "去AI味" / "太AI了改一下" | ai-humanizer |
| "检查一致性" / "有没有矛盾" | consistency-tracker |
| "拷打大纲" / "拷打章节" / "深度拷问" | grill-with-docs |

## Skill Pipeline

```
构思 → 世界观 → 角色 → 大纲 → [拷打大纲] → 逐章写作指令
                                    ↓
                   逐章循环: [写 → 拷打章节 → 审 → 去AI味 → 一致性检查 → 审批]
                                    ↓
                              整本编译输出
```

## File Naming Conventions

每本小说在 `novels/<书名>/` 下有独立的文件。当前工作目录应切换到小说目录下。

- 世界观：`world_settings.md`
- 角色档案：`characters.md`
- 剧情大纲：`plot_outline.md`
- 写作指令：`chapters/briefs/NNNN.md` + `chapter_briefs.json`
- 章节正文：`chapters/chapter_NNNN.md`
- 去 AI 味后：`chapters/chapter_NNNN_humanized.md`
- 审核报告：`reports/chapter_NNNN_quality.md`
- 实体数据库：`.storyai/entity_db.json`
- 一致性报告：`consistency_report.md`
- 流水线状态：`.storyai/state.json`
- 小说特定规则：`.storyai/novel-rules.md`

## Scripts

所有脚本使用 Python 3.8+ stdlib，零 pip 依赖。脚本做确定性计算（字数统计、正则匹配、JSON 解析），Claude 做创意生成和判断。

关键脚本：
- `quality_check.py` — 33 维度质量审核引擎
- `detect_patterns.py` — 51 条 AI 腔模式扫描器
- `parse_outline.py` — 大纲到逐章写作指令的转换器
- `ai_detector_quick.py` — AI 标记词快速检测（Top-20）
- `extract_entities.py` — 章节实体信息提取
- `validate_structure.py` — 世界观文档完整性验证

## Language Conventions

- 所有小说输出为**简体中文**
- 代码注释和变量名可以使用英文
- 文档和模板使用中文
- 章节格式：纯中文，数字用中文写法（一、二、三十），无阿拉伯数字，无英文

## Quality Standards

- **AI 标记词零容忍**：51 条 AI 腔模式 + 10 条去 AI 化规则（详见 `shared/references/ai_patterns.md` + `shared/references/deai_rules.md`）
- **5 层 33 维度审核**：基础指标 → 文风结构 → 内容大纲 → 格式规范 → 高级分析
- **一致性保证**：跨章实体追踪，防止时空/规则/角色矛盾
- **去 AI 味双层机制**：写作时约束注入（预防）+ 写后扫除打磨（治疗）

---

## 拷打式评审（Grill）强制规则

**大纲编辑和章节写作完成后，必须进行拷打式深度评审。** 这不是可选项——是写后检查的第 0 步，排在 13 项检查之前。

### 触发条件

以下任一操作完成后，**必须**执行拷打：

| 操作 | 拷打对象 | 时机 |
|------|---------|------|
| 新建/修改大纲 | 大纲整体结构 + 每卷情节逻辑 | 大纲定稿前 |
| 写新章节 | 章节内容 | 写完即拷打，在 13 项检查之前 |
| 修改章节 | 修改部分 + 关联段落 | 修改完成后 |
| 新增/修改角色 | 角色动机 + 角色关系 + 对大纲的影响 | 角色定稿前 |
| 新增/修改世界观 | 规则一致性 + 对已写章节的影响 | 设定定稿前 |

### 拷打原则

1. **不手软**：发现问题直接指出，不使用"可能""或许""可以考虑"等软化措辞
2. **给出方案**：不只说"这里有问题"，同时给出具体的修改建议
3. **追问到底**：如果解释不够充分，继续追问，直到逻辑站得住
4. **一次只问一个**：不连珠炮式地扔 5 个问题。一个问题 → 等反馈 → 下一个问题

### 对大纲的拷打清单

逐项检查，一项不能跳：

1. **情节因果链**：每个事件是否由前一个事件推动？靠巧合推动的关键转折有几个？（≥2 个 = 红灯）
2. **角色动机**：每个核心角色的行为是否有清晰的、个人化的驱动力？动机是否在故事中展示（而非被告知）？
3. **冲突设计**：核心冲突是否足够尖锐？对抗力量是否足够强大？冲突升级的节奏是否合理？
4. **节奏与结构**：高潮和低谷是否交替？连续 5 章以上同一强度 = 节奏问题
5. **伏笔与回收**：关键伏笔是否在后续有明确回收？有没有被遗忘的伏笔？
6. **情感曲线**：每卷/每章的情感强度是否合理？情绪转折是否有铺垫？
7. **时间线**：事件之间的时间间隔是否合理？有没有时间矛盾？
8. **角色弧光**：每个核心角色是否有清晰的变化轨迹？变化是否渐进（而非跳跃）？

### 对章节的拷打清单

逐项检查，一项不能跳：

1. **POV 越界**：每一段——"这个信息主角是怎么知道的？"答不出来 = 违规
2. **对话真实性**：读出对话——像真人在说话还是像朗读论文？有没有口语翻车的 5 个模式？
3. **场景逻辑**：每个场景的发生地点、时间、人物是否合理？
4. **情感真实性**：角色的情绪反应是否与他们的性格和当前处境一致？有没有概括情绪词直给？
5. **事实准确性**：篮球规则、NBA 时间线、真实人物/事件是否正确？
6. **节奏**：本章内部是否有张弛变化？段落长短是否有节奏？
7. **信息量**：逐句删——删了不影响理解的句子就是废话
8. **跨章接缝**：章首是否从章末的下一刻开始？时间参照是否有前章对应？
9. **实体认知越界**：每个角色/实体在当前时间点知道什么？超过知识边界的内容 = 越界
10. **对话主语**：连续从句是否重复主语（"你...你...你..."）？

### 拷打后处理

- 拷打中暴露的问题，**必须在拷打会话中当场解决**
- 解决后才进入 13 项检查流程
- 如果拷打发现了大纲级别的问题（如情节逻辑漏洞、角色动机矛盾），先回退修改大纲，再重新写章

---

## 章节写作标准流程（每次写新章必须执行）

### 写前准备 — 规则必须逐条过，不能只读不记

1. **定位大纲**：读 `.storyai/chapter_outline_volX.md`（优先）或 `plot_outline.md`，找到对应章节的大纲节点
2. **逐条过规则文件**（这条不是"读一下"——是逐条在脑子里过一遍，写的时候每写一段都要回想）：
   - `.storyai/novel-rules.md` — 本小说特定规则（主角名称、语言风格、MEAC 标签禁用、跨章接缝、跟腱描写上限 3 次、"不是X是Y"零容忍、句子间必须有连接词、禁止故作高深、禁止金句收尾、禁止没头没脑台词、禁止重复描写同一特征等）
   - `shared/references/deai_rules.md` — 10 条去 AI 化规则（感官密度、情感展示、比喻具象化、"这意味着"禁止等）
   - `shared/references/ai_patterns.md` — 51 条 AI 腔禁止模式
3. **加载上下文**：读 `world_settings.md` 相关章节、`characters.md` 出场角色档案、前 2-3 章正文（特别是上一章最后 3 段——确认章首从章末的下一刻开始）
4. **确认大纲匹配**：当前要写的章节号和大纲节点是否一致？如果不一致，先用大纲章节号覆盖

### 写作约束（注入到写作 prompt）

**写中自查：每写完一段，脑子里过一遍——有没有蹦出"不是X，是Y"？句子之间有没有连接词？有没有故作高深的短句留白？有没有概括情绪词直给？这些规则必须在写的时候就在脑子里，不是写完了拿来对答案。**

- **POV**：严格跟随主角视角。检查方法：每段问"这个信息主角是怎么知道的？"答不出来就删
- **主角名称**：按 `novel-rules.md` 第 0 条——叙述主体用主角真名，原主身份仅用于物品/文件/他人称呼
- **字数底线**：中文字符 ≥ 2000。写完即跑 `python -c "import re; print(len(re.findall(r'[一-鿿]', open('章节路径',encoding='utf-8').read())))"`
- **破折号上限**：全章 ≤ 15 处。写完即跑 `python -c "print(open('章节路径',encoding='utf-8').read().count('——'))"`
- **AI 标记零容忍**：51 条模式 + 10 条去 AI 化规则（详见 `shared/references/ai_patterns.md` + `shared/references/deai_rules.md`）。severity 3+ 必须清零。写完即跑 AI 标记扫描
- **节奏**：长短句交替，段落长短穿插。有 1 句超短段 + 有 10+ 句长段
- **对话标签**：用动作 Beat 替代"XX 说"。对话有潜台词，不信息倾泻
- **对话主语**：中文口语中连续从句不重复主语。"你说你三个月没训练，你上来用几脚传球就..." ← 三个"你"开头是英文从句习惯。正确："你说你三个月没训练——上来用几次传球就把首发打崩了。是不是..."
- **数字**：章节正文用中文写法，阿拉伯数字仅允许年份/专业数据
- **感官密度**：每千字至少 1 处触感/温度 + 1 处气味 + 1 处非视听身体感觉。详见 `shared/references/deai_rules.md` 规则 1
- **情感展示**：禁止"悲伤、愤怒、恐惧、喜悦"等概括情绪词直给。必须用动作、生理反应或扭曲思维替代。详见 `shared/references/deai_rules.md` 规则 2
- **"不是X，是Y"句式**：一章最多 1 次。详见 `.storyai/novel-rules.md`
- **比喻约束**：禁止概念化比喻（"宛如天使""仿佛末世"），必须用具象的、来自故事世界的喻体。详见 `shared/references/deai_rules.md` 规则 6

### 写后 13 项检查（按顺序，一项不能跳）

**注意：在执行这 13 项检查之前，必须先完成"拷打式评审"（见上方"拷打式评审（Grill）强制规则"章节）。拷打 = 第 0 步，排在所有检查之前。**

| # | 检查项 | 方法 |
|---|--------|------|
| 1 | 大纲一致性 | 标题+内容匹配大纲节点；**逐句排查是否涉及后续大纲节点的事件**——"连胜"章不能藏"八胜两负"，那两句败仗是下一章的内容 |
| 2 | 字数 ≥ 2000 | Python regex `[一-鿿]` |
| 3 | 破折号 ≤ 15 | Python count `——` |
| 4 | AI 标记 51 条 | Python 正则逐条扫描（A1-A6, B1-B5, C1-C6, D1-D6, E1-E6, F1-F6, G1-G6, H1-H5, I1-I5） |
| 5 | POV 约束 | 人工：最后一段最易漂移，逐句确认"主角怎么知道的" |
| 6 | 主角名称规则 | 人工+grep：叙述中是否误用原主名替代主角名 |
| 7 | 代词指代 | 人工：每段多男性角色时，"他"必须明确指向 |
| 8 | 残缺句 | 人工逐句读：单字/单词不能独立成句，缺主语不能连缺三次 |
| 9 | 信息量 | 人工逐句读：逐句删——删了不影响理解的就是废话。同时检查语法完整性：一个句子塞入三个以上独立逻辑块且无连接词 = 语法塌陷 |
| 10 | 中英文混用 | 人工：英文仅在专有名词、缩写、口头禅，不在叙述句混用 |
| 11 | 事实核查 | 涉及真实人物/事件/日期的逐个确认 |
| 12 | 错别字 | 人工逐句读：逐句出声默读，读不顺的就是有问题。注意——大脑会在快速扫读时自动补全逻辑缺口，以段为单位扫读不可靠 |
| 13 | **跨章接缝** | 人工逐句读：同时读上一章最后 3 段 + 本章前 3 段，确认——①结尾与开头的地点/时间一致，无时序倒流；②本章内"昨天""上次""之前"等时间参照在前章有对应事件；③时间跳跃有明确交代 |

**人工检查执行规范**：以上所有标记"人工"的检查项（#5-#13），**必须以句为单位逐句过**，不能以段为单位扫读。大脑处理熟悉文本时会自动补全逻辑缺口——扫读一段话只需几秒，但一句一句拆开读才能暴露语法塌陷、指代漂移、时序矛盾。以 Ch5 Marcus 那句 4 逻辑块杂糅的台词为例：扫读时大脑自动补全了连接词，只有逐句拆才能发现"那不是一个 D1"与前后文没有语法关系，整句根本不是一个有效的汉语句子。

### Python 一行自动化检查命令

```bash
cd F:/StoryAI && python -c "
import re; text = open('novels/<书名>/chapters/chapter_XXXX.md', encoding='utf-8').read()
cjk = len(re.findall(r'[一-鿿]', text))
emdash = text.count('——')
print(f'字数:{cjk} 破折号:{emdash}')
patterns = [('心中涌起|涌上心头|心底泛起|心中一', 'C1-心中'), ('不由得|不禁|忍不住|下意识地|不自觉地', 'C2-不由得'), ('叹了口气|苦笑|摇了摇头', 'C3-叹气苦笑'), ('感到一阵', 'C4-感到一阵'), ('目光闪烁|眼神一凝|眼中闪过|眸光|目光复杂', 'C5-目光'), ('嘴角扬起|嘴角上扬|嘴角抽搐|嘴角一抽|嘴角勾起|嘴角.{1,5}[翘动咧]', 'C6-嘴角'), ('重要意义|重要作用|标志着|新阶段|注入.*活力', 'A1-拔高'), ('值得一提的是|值得关注的是|不可否认|不容忽视|毋庸置疑|众所周知', 'A2-空洞'), ('体现了|彰显了|折射出', 'A4-尾巴总结'), ('也就是说|换句话说|简单来说|换言之|总而言之', 'B1-复述'), ('不知道的是|殊不知', 'H1-不知道'), ('命运的齿轮', 'H2-命运齿轮'), ('暗流涌动|暴风雨前的宁静', 'H3-暗流'), ('才刚刚开始|真正的.{1,10}还在后面', 'H5-才刚开始'), ('淡淡道|冷冷道|沉声道|苦笑道', 'G2-冷道')]
for pat, name in patterns:
    hits = re.findall(pat, text)
    if hits: print(f'  {name}: {hits[:3]}')
"
```

### 常见翻车点

- **大纲跳章**：写了 3 章覆盖了大纲 20 章的内容。一章节 = 一大纲节点，不要压缩。
- **POV 漂移**：最后一段突然切换到教练/队友视角写内心活动。主角不在场的内心活动 = 违规。
- **破折号爆炸**：习惯性用 `——` 当逗号用。写完先跑破折号计数，超标就批量替换为 `，` 或 `。`。
- **硬编码章节号**：写作时不要假设"下一章是第几章"。先看大纲确认当前写到哪个节点，再映射章节号。
- **跨章时序断裂**：章末角色已离开房间，章首倒回房间（Ch1→Ch2 真实案例）。章末角色刚躺下等天亮，章首凭空引用"比昨天快了一秒"——重生后的时间线没有昨天（Ch2→Ch3 真实案例）。修复方法：写新章前必须先读上一章最后 3 段，从那里接续。章内的时间参照（"昨天""上次""刚刚"）必须在之前章节中有对应事件。
- **实体认知越界**：角色描述其他实体（人、球队、群体）的所知所想时，必须检查该实体在当前时间点是否可能知道这件事。Ch11 真实案例——赛季还没开打，大 D 说"现在他们在讨论怎么防我们"：别的球队一场比赛都还没看到，不可能在讨论防他们。修复：改为内部认知——"现在我们在更衣室里讨论怎么赢……等到赛季开打，他们会发现的。"检查方法：写完后把每个角色/实体拉出来单列一行，问"这个时间点他知道/看到/经历了什么？"，超过这个范围的内容就是越界。
- **单字动词卡顿**：中文叙事中孤立的单字动词（停、看、想、说、走）放在句末会破坏语流。"全队就停了"→"全队就卡住了"，"我们会停的"→"我们的进攻会卡住的"。写成双音节词或加宾语——"停下来""停住""卡住了""停顿了一下"——让音节自然收束。检查方法：默读时如果句末单字让你的呼吸顿了一下，加一个字。
- **统计单调性翻车**：同一场比赛内，累积统计量（失误、得分、篮板、助攻、犯规）只能增加不能减少。Ch16 真实案例——"在前半节失误了三次"+"终场零失误"= 逻辑矛盾。修复：将中间描述改为非统计性描写（"被包夹逼得三次停球"），或确保终值 ≥ 中间值。检查方法：提取所有带时间标记的统计量，逐对验证单调性。详见 `shared/rules/chinese-novel-writing.md` 第 7 节。
- **战级数学翻车**：战绩数字做加法对不上。Ch17 真实案例——"十胜一负，在第十二场输了"：10+1=11 ≠ 12，且"首败"章节赛前不能是十胜一负（已输过一场）。检查方法：写每场比赛前记下当前战绩，写完验证 胜+负=已打场次。首败章赛前必须全胜。
- **对话口语翻车**：角色说话像朗读论文/标语/英文直译。Ch17 真实案例——"不成体系""明天的你们和今天一样""接球之前弯"——五个模式：①孤立单/双字应答 ②及物动词缺宾语 ③英文直译句式 ④学术词侵入口语 ⑤标语腔。检查方法：出声朗读对话，读不顺就有问题。详见 `shared/rules/chinese-novel-writing.md` 第 9 节。
